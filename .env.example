# Public API base URL for the Go backend.
# Used by lib/api.ts both server-side (Server Components / Server Actions)
# and client-side (browser fetch). Cookies are sent with credentials: include.
NEXT_PUBLIC_API_BASE=http://localhost:8376
